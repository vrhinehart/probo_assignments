To run a file, simply run: `uv run -m day_x.filename`  
Note the -m for "run as module" and the lack of a .py extension.

The only dependency is you need `uv` installed on your system.  
https://docs.astral.sh/uv/
